import React from 'react';
import Input from './index';

export default {
  title: 'Atoms/Input',
  component: Input,
  tags: ['autodocs'],
};

export const Default = () => <Input placeholder="Email" value="" onChange={() => {}} />